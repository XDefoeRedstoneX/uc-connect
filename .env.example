# Supabase (required)
# Use values from your hosted Supabase project dashboard.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only (recommended for API routes / SSR)
# Keep this private and do not expose it to the browser.
SUPABASE_SERVICE_ROLE_KEY=
