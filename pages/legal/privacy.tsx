import SiteLayout from "@/components/SiteLayout";

export default function PrivacyPage() {
  return (
    <SiteLayout title="Privacy Policy | UC Connect">
      <section className="card">
        <h1>Privacy Policy</h1>
        <p>We collect account and profile data only for service delivery, authentication, and directory features.</p>
        <p>Data requests and account deletion support are available via the support page.</p>
      </section>
    </SiteLayout>
  );
}
